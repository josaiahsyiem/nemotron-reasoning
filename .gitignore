# Secrets — never upload API keys
.env
# Data — never upload competition data
*.csv
# Generated traces — derived from competition data, keep local
*.jsonl

# Python junk
__pycache__/
*.pyc
.venv/
venv/

# Model files — too big for git
*.safetensors
*.bin
*.pt